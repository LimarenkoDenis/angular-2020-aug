import { ResultAmountPipe } from './result-amount.pipe';

describe('ResultAmountPipe', () => {
  it('create an instance', () => {
    const pipe = new ResultAmountPipe();
    expect(pipe).toBeTruthy();
  });
});
